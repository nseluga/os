---
name: dev-team-learnings
description: "Generalizable /dev-team + /dev-team-auto process learnings — orchestration, track/model, QA/test, and review patterns that apply across any repo"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 7037cedd-2c09-4470-a67b-fc6af17b22bb
  modified: 2026-07-28T13:21:33.974Z
---

Cross-project lessons from dev-team runs. Project-specific findings stay in each repo's `.claude/dev-team/team-memory.md`.

- 2026-07-09 When a change adds `<details>/<summary>` elements inside repeating components (cards), pre-existing tests that use first-match regex for `<details>` will silently match the wrong element — **Why:** regex position assumptions break when more elements of the same type are added. **How to apply:** fixture tests involving HTML elements that may appear multiple times (details, button, summary) should match by content (e.g. "Completed" text, aria-label) rather than by DOM position.

- 2026-07-09 `readManual()` must use explicit per-field construction — never `{ ...EMPTY_MANUAL, ...parsed }` — **Why:** shallow spread returns references to the constant's nested objects; callers that mutate those objects (e.g. `manual.hidden_fields[id][field] = true`) poison `EMPTY_MANUAL` for all subsequent calls in the same process, causing hard-to-reproduce state bleed. **How to apply:** always construct `{ field1: {...(parsed.field1 ?? {})}, field2: [...(parsed.field2 ?? [])], ... }` explicitly. Any new field added to the data model must be added to this construction simultaneously.

- 2026-07-09 Expensive I/O (git shell calls via getMergedProjects/getProjects, disk reads) must never run inside a write mutex — **Why:** the lock serializes all concurrent requests for the full duration of the slowest call; a git subprocess can take seconds. **How to apply:** resolve any external data (project list, git log) before `runExclusive`; the mutex should protect only the read-modify-write step on the shared file.

- 2026-07-09 Before a color palette sweep, audit test files for color-anchored assertions (`/text-gray-/`, `/text-blue-/`, etc.) and update them in the same commit as the sweep — **Why:** the sweep will break those assertions, and the fix-as-follow-up burns an extra QA attempt. **How to apply:** run `grep -r 'text-gray-\|text-blue-' tests/` before executing the sweep; patch the assertions alongside the component changes.

- 2026-07-09 `focus-visible:ring-*` on filled colored buttons must use a white ring with offset, not a lighter tint of the button color — **Why:** `ring-indigo-400` on `bg-indigo-600` achieves ~2.4:1 contrast, below the WCAG 3:1 minimum for non-text UI components. **How to apply:** use `focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-[button-color]` on any filled-color button.

- 2026-07-09 Keyword-matching functions (autoTag, search, filter) must use word-boundary checks, not bare `indexOf` — **Why:** substring matches cause false positives ("os" matches inside "gross", "ml" inside "email", "ai" inside "said"). **How to apply:** implement `isWordBoundary(text, start, end)` checking `\W` or string edge on both sides of the match; never ship a keyword scanner that uses only `indexOf`.

- 2026-07-09 When composed SSR pages need both getMergedProjects() output AND raw manual data (token_log, notes, etc.), use an optional `manual?` parameter on getMergedProjects() to share a single disk read — **Why:** without this, getMergedProjects() calls readManual() internally while the page also calls it directly, doubling disk I/O per render. **How to apply:** `const manual = await readManual(); const projects = await getMergedProjects(manual);` — one read, zero redundancy.

- 2026-07-09 `claude -p --bare` removes the Skill tool from the model's tool list entirely — **Why:** `--bare` strips all non-essential tools including the Skill invocation mechanism; any eval rung that relies on skills silently gets zero skill access, not degraded access. **How to apply:** never use `--bare` for eval rungs that test skill-augmented behavior; use non-bare mode with `CLAUDE_CONFIG_DIR` + `ANTHROPIC_AUTH_TOKEN` + an isolated skill dir instead. Probe the flag empirically before building against it.

- 2026-07-09 Headless one-shot eval tasks must use pure unit tests, not integration tests that require a running dev server — **Why:** a check.sh that needs `npm run dev` to be live cannot pass in a subprocess that exits immediately; the task silently fails on infra, not logic, poisoning the result matrix. **How to apply:** when authoring eval tasks, verify the check runs to completion with the server stopped; if it doesn't, either convert to a pure unit test or mark the task as unsuitable for headless evaluation.

- 2026-07-17 When a correction pass updates source files, downstream summary surfaces (about pages, llms.txt, READMEs, config descriptions) are the most common miss — **Why:** engineers scope their grep/edit to the files that contain the error, not every file that echoes the same value. A correction in `pitcher-injury-risk.mdx` leaves `about.astro` and `llms.txt` stale. **How to apply:** for any pass that corrects factual claims, explicitly instruct the engineer to grep all summary/description surfaces for the old value and update them in the same commit.

- 2026-07-17 Pattern sweeps (em dash, naming, style, banned vocab) must cover ALL authored content files, not just published/active ones — **Why:** draft:true or hidden files still contain authored prose subject to the same voice rules; scoping the sweep to live files causes QA to fail on the broader grep criterion. **How to apply:** run pattern greps over the full content directory (`grep -rn 'pattern' src/content/`), never a filtered subset; the done-when criterion should name the full directory, not a page list.

- 2026-07-19 In a truth-in-marketing / copy-correction item, an engineer reframing claims will tend to assert an unbuilt capability as a present-tense guarantee (e.g. "you can export your data any time" when no export feature exists) — **Why:** the reframe fixes the flagged false claim but introduces a new one because the engineer reasons about desired positioning, not shipped features. **How to apply:** for any content/marketing item, add a review check that every present-tense capability claim maps to a feature that actually exists in the repo; phrase not-yet-built capabilities as intent/service ("we'll export it whenever you ask"), never as a self-serve guarantee. Grep the repo for the claimed capability to confirm it exists before passing the item.

- 2026-07-19 An exported config/constant that serves as a shared source-of-truth must be DEEP-frozen at runtime, not just typed `Readonly<>`/`as const` — **Why:** TypeScript `Readonly` and `as const` are compile-time only; nested object fields stay runtime-mutable, so any consumer can poison the shared module for the whole process (`PRICING.standard.monthlyCents = 1`). Same failure family as the EMPTY_MANUAL shallow-spread bug, in a new form. **How to apply:** for any module-level constant that is imported and read by others (pricing configs, default option objects, enum-like maps), `Object.freeze` the top object AND each nested object, mark fields `readonly`, and add a test asserting a mutation attempt does not change the value. Flag this in review for every new shared package.

- 2026-07-19 dt-* agents write their `*-report.md` to the working tree but often do NOT commit it; the orchestrator must commit reports itself when recording each item's outcome, and must never `git checkout`/discard a report file expecting the latest agent's version to persist — **Why:** in an A2 run the engineer-report was written but not committed with the feature commit, then a `git checkout --` to undo a different agent's stray edit destroyed it, forcing a manual reconstruction; a mid-run agent that dies (API socket error) also leaves reports stale/uncommitted. **How to apply:** treat `.claude/dev-team/*.md` as orchestrator-owned artifacts — `git add .claude/dev-team/ ` in the same commit that records the PROGRESS/PLAN outcome; before discarding any uncommitted working-tree change, confirm it isn't the only copy of a just-written report. Also: any agent that starts a dev server must run it backgrounded with a bounded curl-poll and always kill it (no orphan servers). **Sharpened 2026-07-27:** this recurred with a *source* file, not a report — QA reverted its own deliberate mutation with `git checkout -- <test file>` and destroyed the Engineer's uncommitted fix to that file, even though the repo's own standing notes already carried the warning. A prohibition in memory is not a control. Put the mechanism in the spawn prompt of any agent told to mutate-and-restore: `cp` the file to a backup, restore from the backup, verify with `shasum -c`, never `git checkout --` on a tree with uncommitted teammate work. The orchestrator should diff any reconstructed file against the original (still in its context) before committing — recovery worked here only because the orchestrator had reviewed the original diff.

- 2026-07-20 For in-place migration edits (no data exists yet), keep every DDL change idempotent with guarded `DO $$`/`IF NOT EXISTS`/`DROP ... IF EXISTS` blocks AND wire Critical-invariant regressions (FK on-delete behavior, status-gating triggers) into the EXISTING local-DB test harness rather than a new test file — **Why:** a schema whose "fix" isn't re-apply-safe breaks `supabase db push`/`db:test:local` idempotency on an already-applied DB, and a Critical caught by review (cascade-delete history loss, a declined record becoming a live job) silently regresses later unless an executable invariant guards it. **How to apply:** when a data-path review returns Critical schema findings, have dt-fix edit migrations in place with guarded blocks, then add a harness assertion that the bad operation RAISES (delete-restrict, confirmed-only insert) and a good one succeeds; re-run the harness as the QA re-gate. Prefer FORCE (not ENABLE) RLS and integer-cents money — Opus review reliably catches these on flag:data-path items, so budget the Opus review there.

- 2026-07-21 A DB helper that reads a JWT/session claim and casts it (e.g. `nullif(claim,'')::uuid`) must gate the cast so a malformed (non-empty, wrong-format) value resolves to NULL, never throws — **Why:** RLS/policy helpers run on every query; a raw `::uuid`/`::int` cast on a garbled or tampered claim throws `invalid input syntax`, breaking every query for that user (a self-inflicted DoS) instead of failing safe to zero-rows. Absent/blank claims usually already fail safe; malformed ones are the gap. **How to apply:** regex-gate the value before casting (`case when v ~ '^[0-9a-fA-F]{8}-...$' then v::uuid else null end`) keeping the function pure/STABLE; add a fixture impersonating a user whose claim is `"not-a-uuid"` and assert zero rows + no throw. Absent, blank, AND malformed must all fail closed.
- 2026-07-21 Supabase RLS is testable on a vanilla local Postgres by emulating the auth layer — **Why:** policies reference `auth.uid()`/`auth.jwt()` and roles `anon`/`authenticated`/`service_role` that don't exist in stock PG, so teams wrongly assume RLS can only be tested against a live Supabase. **How to apply:** in the ephemeral test cluster create `schema auth` with `auth.jwt()` = `coalesce(current_setting('request.jwt.claims',true),'{}')::jsonb` (+ `auth.uid()`), create the three roles, and impersonate per-case with `select set_config('request.jwt.claims', <json>, true); set local role authenticated;`. NOTE PG version: `security_invoker` views are PG15+ (Supabase) and error on PG14 — gate those cases on `server_version_num>=150000` and treat base-table RLS + column GRANTs (version-independent) as the real, fully-testable guarantee.

- 2026-07-21 Route-middleware gating gives false authz confidence in Next.js — server actions are POSTable from ANY path, bypassing the matcher. **Why:** engineer shipped 6 unauthenticated mutating actions behind a "protected" /admin matcher; QA's happy-path smoke missed it, only review caught it. **How to apply:** in any Next.js repo, reviewers/QA must verify each mutating server action checks authz itself (shared fail-closed helper as the first statement); never accept middleware as the guard.

- 2026-07-21 Every agent that runs the test suite must be told the exact expected pass/skip counts and how to wire env (e.g. DATABASE_URL from .env.local) in its spawn prompt — not just QA — **Why:** spawned agents get fresh shells; env-gated integration files skip silently, so a builder/UI agent reports "tests pass" while the DB-backed half never ran (recurred twice in one run: fixer item 8, dt-ui item 10). **How to apply:** the orchestrator states "expect N pass / M skip; if skips exceed M, your env is wrong — fix it before committing" in every spawn prompt that includes a test run.

- 2026-07-21 JS money-path guards must use `Number.isSafeInteger`, never bare `Number.isInteger` — and reviewers' prescribed fixes can themselves be wrong on fractional intermediates. **Why:** `Number.isInteger(2**53)` is true, so an "integer cents" assert built on it silently admits precision-lost values; the gap threads through single-value asserts, post-reduce subtotals, AND `total * pct` products (which overflow 2^53 before the /100). A reviewer prescribed `isSafeInteger(product)`, but the product is legitimately fractional (pct 66.67) — the correct guard there is magnitude (`product > MAX_SAFE_INTEGER`). Adversarial "overflow" probes that deliberately stay in safe range never catch any of this. **How to apply:** on any flag:money item, check every cents assert uses isSafeInteger, guard sums after the reduce and products before division (magnitude check when fractional), and require at least one probe that actually crosses 2^53; when a fixer deviates from a prescribed money-path fix with reasoning, re-gate with a scoped re-review (parallel with scoped QA) rather than accepting or full-looping.

- 2026-07-22 A clean `tsc` + `next build` does NOT prove client-side hooks exist at runtime — an engineer used React 19's `useActionState` on a React 18 repo; types/build passed, both editor routes 500'd in production, and only QA's live smoke caught it — **Why:** bundlers/types can resolve an API that the installed runtime lacks, and unit tests rarely execute client hook wiring. **How to apply:** builders must copy an existing working form/component's hook pattern from the SAME repo before writing a new one; QA's live smoke must hit every NEW route (not just changed ones) even when the suite is green. Also: any UI-polish agent given a "token-only colors" rule needs an executable grep for raw hex in its re-gate — the instruction alone was violated the same run it was given.

- 2026-07-22 A Next.js public page that displays admin-toggled DB content (published banners, feature flags, announcements) will silently never update if it is statically prerendered — build, typecheck, and unit tests all pass while the page is baked at build time with the toggle off — **Why:** App Router defaults pages with no dynamic APIs to static; the DB query runs once at build, so request-time renders never re-query, and only a live smoke (publish → GET page) catches it. **How to apply:** when an item wires DB-driven content into a previously-static page, the builder must set the rendering mode explicitly (`force-dynamic`, `revalidate`, or revalidatePath-on-mutate) and QA's live smoke must assert the toggle round-trip against the built (`next build && next start`) server, not the dev server.

- 2026-07-22 Any dedupe/identity key built from an email address must canonicalize (`lower(trim())`) in BOTH the app-side key builder and the SQL grouping, via one shared helper — and "remind N days after last event" schedulers should derive eligibility from the DB at fire time (cron scan on MAX(event_at)) rather than cancel/reschedule queued jobs — **Why:** raw `contact_email` split `Bob@x.com`/`bob@x.com` into two identities, defeating a PK-based send ledger (double-send) — caught only by Opus review, not QA; and pg-boss `singletonKey` REJECTS duplicate enqueues rather than retiming the pending job, so event-scheduled supersede designs silently fail the "newer event pushes the reminder out" invariant. **How to apply:** on any flag:data-path item keying on user-supplied strings, review every key builder AND every SQL GROUP/DISTINCT site for canonicalization drift (test: mixed-case variant collapses to one row); prefer scan-derived eligibility for last-event-relative schedules; verify a queue library's singleton/replace semantics from its types/docs before betting a correctness invariant on them.

- 2026-07-22 A QA test asserting a NEGATIVE side effect ("no ledger row is written when X") must drive the real production entry point, not re-derive the property from adapter calls — if the function under test is unexported, export a test seam (or drive it through the queue/route) rather than testing around it. **Why:** QA "confirmed" skip-without-claim by checking the adapter returned null and the ledger was empty, without ever invoking the handler; the handler actually claimed the key before the check (its comment lied), and only review caught it. A passing absence-assertion that never executes the guarded path validates the spec, not the code. **How to apply:** in QA spawn prompts for any ordering/absence criterion, require the test to call the actual handler/action and name the seam; reviewers should cross-check code comments claiming ordering guarantees against the literal statement order.

- 2026-07-22 When a plan item requires touching live hosted infra (applying a migration, hitting a DB port), the orchestrator should probe the environment's actual capability itself BEFORE gating — try the ports/tokens, then state the adjudicated constraint in QA's spawn prompt ("X is impossible from this machine; the correct handling is loud skip-with-note + auto-activate flag + accurate PROGRESS deferral; overclaiming = FAIL") — **Why:** an inherited live-smoke item had an un-appliable migration (all PG ports egress-blocked, no management token); without up-front adjudication QA would have FAILed on an environment fact no builder can fix, burning attempts toward a false BLOCKED. **How to apply:** 2 minutes of nc/env probes by the orchestrator converts an unfixable criterion into a verified-degraded gate; require the suite to probe the capability at runtime and auto-activate the assertion when provisioning lands, and require PROGRESS to keep that check DEFERRED rather than claiming live verification.

- 2026-07-22 A form control that constrains choices client-side (dropdown/calendar of "valid" options) is often the ONLY enforcement of that constraint — the server action validates format, not membership — **Why:** replacing a booking date <select> with a calendar surfaced that a crafted POST could submit any blackout/full/out-of-window date; the gap predated the item and neither the engineer nor QA's happy-path caught it (review did). The right fix is re-running the same server-side rule engine that fed the control (full availability check), not the reviewer's bounds-only prescription; fail open only in the explicitly-degraded (adapter-unavailable) mode. **How to apply:** on any item that builds or swaps a choice-constrained input, have review verify the server re-validates membership in the offered set via the same data source, and have QA add a crafted-POST probe through the real action entry point.

- 2026-07-22 Repo-wide grep/sweep tests (secret isolation, banned patterns) must exclude `.claude/` — nested dev-team worktrees are full checkouts of the same repo, so a self-scan "passes" in a worktree but fails in the main checkout whenever any sibling worktree exists — **Why:** the dev-team constantly creates worktrees under `.claude/worktrees/`, so the failure recurs and masquerades as a security regression. **How to apply:** add `.claude` to every sweep test's skip list at authoring time; when a sweep fails only in the main checkout, check for nested-worktree paths in the diff before debugging code.
- 2026-07-24 In any Next.js repo, `unset`-ing env vars before starting the dev/prod server does NOT override them — Next's env loader repopulates absent keys from `.env.local` at boot, so a live smoke meant to force a fallback adapter silently hits the real backend (QA saw a success screen while nothing landed in the test DB) — **Why:** absent ≠ overridden for Next's layered env resolution; only a present-but-empty value wins. **How to apply:** export the vars as EMPTY STRINGS (`NEXT_PUBLIC_X="" pnpm dev`) when a smoke test must defeat `.env.local`, and have QA verify the row actually landed in the intended DB (psql count), never trust the UI success state alone.

- 2026-07-22 Parallel-group item branches conflict only on `.claude/dev-team/*-report.md` (add/add — each orchestrator writes its own reports); this is expected, not a sign the group overlapped — **Why:** report files are per-item artifacts on every branch; treating the conflict as a real overlap would wrongly demote the group to sequential. **How to apply:** resolve report-file conflicts with `--theirs` (latest item's reports) and merge on; only a conflict in source/test files means the group wasn't disjoint.

- 2026-07-24 In the Next.js App Router, a client component rendered by a server component and fed a URL-derived prop (`?date=`, `?tab=`) keeps its existing DOM across soft navigations — a bare `defaultValue={prop}` is correct on first load and silently STALE after every `<Link>` click; and `useRouter()` from `next/navigation` throws "expected app router to be mounted" inside `renderToStaticMarkup` render tests, which have no AppRouterContext. **Why:** both failures pass typecheck, build, and every markup assertion — the staleness is a client-reconciliation behavior no SSR string test can observe, and the router one only fires in the test harness the repo already relies on for admin pages. **How to apply:** tell the builder to use `key={prop}` (remount, keeps the input uncontrolled — no controlled-value flicker) or a fully controlled `value={prop}`, and to prefer `window.location.href` over `useRouter` in any component reachable by static render tests; since jsdom is often absent, have QA (a) assert the guard structurally off the returned element (`DateJumpInput({date}).key === date`) and (b) **mutation-test it** — delete the guard, confirm the test fails, restore. Handler logic needs no jsdom either: call the component as a plain function and invoke `el.props.onChange({target:{value}})` with a stubbed `globalThis.window`.

- 2026-07-25 A library's deprecation `console.warn` is NOT evidence that the feature is gated off for you — most SDKs emit it unconditionally from the constructor for every caller — and when a plan item's premise is "X is broken", the orchestrator should spend ~5 tool calls driving the real thing BEFORE spawning any builder. **Why:** an item premised on "Places autocomplete popup absent / probably needs migrating to the new API" carried an escalation clause to full track; a live browser drive showed the legacy widget returning real predictions *while* logging "not available to new customers", which killed the migration hypothesis, kept the item on light track, and stopped an engineer from rewriting a working data path. The dt-research brief had led with "if the warning is present, migrate" — research reports the vendor's *policy*, it cannot observe *your* project's entitlement. **How to apply:** for any "feature X doesn't work" item, the orchestrator personally reproduces first (browser drive, curl of the actual asset, one psql query) and states the adjudicated root cause in the builder's spawn prompt as ground truth; treat vendor deprecation warnings as noise unless behavior also fails, and fetch the competing config variants directly (e.g. both loader URL shapes) to prove a prescribed fix is safe before anyone writes code. Corollary: agents' prescribed "hardening" fixes need the same tracing as their bugs — QA here prescribed resetting a re-entrancy guard in a cleanup, which would have introduced a StrictMode double-attach.

- 2026-07-25 When QA reports a WORKAROUND it used to trigger an interaction ("I had to focus the element and press Enter because the click target was tiny"), treat that workaround as a finding, not a footnote — the gate can legitimately PASS while the feature's primary gesture is nearly unusable. **Why:** an item whose headline criterion was "clicking a day cell opens a popup" passed every functional check via keyboard, and only QA's closing methodology note revealed that an invisible full-cell trigger at `z-index:0` was ~6px mouse-clickable, because a sibling list at `z-index:1` covered the cell — z-order puts a control *behind* a sibling's whole BOX, not just behind its visible children, so gaps/padding/labels of that sibling all swallow the click. **How to apply:** orchestrators should read QA's "how I drove it" prose as carefully as its verdict and re-open any criterion QA satisfied by a path a real user wouldn't take; the fix for an overlay-under-a-sibling is `pointer-events:none` on the covering container plus `pointer-events:auto` on its real controls, which leaves the z-index layering (often source-guarded) untouched. Budget a dt-ui pass with live real-mouse verification for any new click-target geometry — a render test and a keyboard-driven smoke both miss it. **Sharpened 2026-07-28:** the positive check that actually settles "a click anywhere navigates" is a `document.elementFromPoint()` sweep of ~11 points per target (4 inset corners, 4 edge midpoints, center, one in a padding gap between text blocks, one over body text), each asserted to `closest('a')` back to the intended control — it runs at CDP level, so it is immune to the OS-focus flakiness that makes real automated mouse clicks intermittent in a sandboxed browser. Require BOTH, and require QA to report which failure signature it saw when a real click does not land: **zero** capture-phase events + `document.visibilityState === "hidden"` means the OS never delivered the input (environment artifact, not a defect), whereas an event landing on the WRONG node is the overlay bug. Without that distinction an orchestrator either false-FAILs a working feature or waves through a real one. Cheapest prevention is upstream: have the BUILDER audit every decorative absolutely-positioned layer in the section for `pointer-events-none` and report what it found, before QA ever runs.

- 2026-07-25 A native `<dialog>`'s `open` state lives in the DOM, not React state, so it SURVIVES an App Router soft navigation — any link inside a modal must close it explicitly or the destination renders invisibly behind the still-open `::backdrop`. **Why:** clicking a job card inside a popup correctly soft-navved and correctly rendered the detail panel, but the dialog never closed, so the "working" feature looked broken; React reuses the same DOM node across the nav, and the usual `event.target === dialogRef.current` backdrop check never matches an inner anchor. **How to apply:** delegate one `onClick` on the dialog's inner wrapper (`if (e.target.closest("a")) dialog.close()`) — server-rendered `next/link` children passed through a `children` slot cannot carry a callback across the server/client boundary, so delegation is the only option; put it on the outer inner-wrapper rather than the body so footer/header links are covered too. Native `<dialog>` + `showModal()` remains the right default (free Escape, focus containment, inertness, `::backdrop`, no dependency, no hand-rolled focus trap), and React 18.3.1 does wire `onClose`/`onCancel` on it.

- 2026-07-25 When a criterion is "X must be UNCHANGED from before", the only honest gate is a true A/B against the actual pre-change build — `git stash` the source files → rebuild → measure → `stash pop` → rebuild → measure, under identical conditions — never a post-hoc arithmetic justification of an observed delta. **Why:** QA PASSed a "no-banner homepage layout is unchanged" criterion while reporting a 41.48px height difference, explaining it as a viewport artifact (`min-height:68vh` measured at vh 774 vs the baseline's 835). The arithmetic was exactly right (61px × 0.68 = 41.48), which is precisely what makes it dangerous — a plausible, correct-looking explanation is indistinguishable from a real regression that happens to be explainable, and the orchestrator cannot audit it without redoing the measurement anyway. Re-run as an A/B it produced `DIFFERING_KEYS: []`. **How to apply:** in QA spawn prompts for any unchanged/identical/no-regression criterion, require the before-leg to be *measured on the reverted code in the same session and same environment*, and state that explaining a delta is a FAIL where eliminating it is available; treat any "the difference is accounted for by …" in a PASS verdict as an unfinished check and redo it yourself. Corollary for geometry work: an element's occupied height includes its borders, so a clearance token derived from an inner content-row height (`.inner{height:72px}`) is short by the border width when the wrapper also has `border-bottom:1px` — offsets computed from it overlap by exactly one pixel, which every source-level check and most eyeballed screenshots will pass.

- 2026-07-25 A "must sit above/below X in stacking order" criterion is VACUOUS whenever the two elements never overlap in normal flow — the only honest gate is to force an overlap at runtime (`el.style.marginBottom='-140px'`), hit-test with `document.elementFromPoint()` inside the overlap region, assert the right element is returned, then restore and re-assert the ORIGINAL rect to prove the probe was non-destructive. **Why:** reading the CSS and asserting intent passes trivially, and a z-index that is never exercised is indistinguishable from one that is wrong; the same repo had already shipped a control that was only ~6px clickable because a sibling's BOX (not its visible children) covered it, which no source-level check caught. Related trap: when two siblings share the same z-index there is no value strictly *between* them, so a criterion demanding A > new > B is unsatisfiable without also raising A — check for that before telling a builder "just add z-index". **How to apply:** in QA spawn prompts, spell out the forced-overlap probe and require the restore-and-recheck; when auditing a layering fix, confirm the raised element's rect intersects nothing that would repaint differently (a full-bleed background at z-index 0 stays below at any positive value, so raising a sibling over it is provably paint-neutral, not merely "probably fine"). Pair it with a static test asserting the z-index ORDERING (a < b < c), not the literal numbers.

- 2026-07-25 A dt-research brief describes the REGISTRY, not your installed tree — require the builder to verify the recommendation against `node_modules`/the lockfile before building on it, and treat "the brief said X" as a hypothesis, never ground truth. **Why:** a brief researched via `npm view` recommended `@astrojs/markdown-remark` + `rehype-sanitize`, but the repo's installed `astro@7.0.7` had replaced that engine with `@astrojs/markdown-satteri` (a native Rust processor with an incompatible plugin API) — every line of the recommended integration would have failed. The engineer only caught it because it ran throwaway scripts against the actually-installed package instead of trusting the brief; a builder that codes to the brief first would have burned a full QA cycle discovering it. This sharpens the existing "research reports vendor policy, not your entitlement" bullet: research also cannot see your *versions*. **How to apply:** put one line in every builder spawn prompt that ships a research brief — "verify the recommended package exists at the recommended major in `node_modules` before writing code against it; if the installed stack differs, design for what is installed and record the override." When a builder does override a brief, the orchestrator should confirm the claim itself (one `ls node_modules/<scope>/` + a version print) before letting it into a commit.

- 2026-07-25 When a rule decides both what gets LISTED and what is REACHABLE, the guard must own it — a filter applied only in the lister leaves the direct-access path wide open, and once both sites exist they drift on matching semantics. **Why:** an excluded-directories list (`library/`, `raw/`) was enforced only in the tree walk, so `?file=library/x.md` returned 200 and rendered content the page's own subtitle claimed to exclude (Important, caught by review not QA — QA had verified the *listing* was correct, which it was). The fix then made the guard case-INsensitive while the walker stayed case-sensitive, so a `Library/` dir would list in the tree and 400 on click — the same class of bug, reintroduced by the fix for it, caught only by a scoped re-review. **How to apply:** for any item with a visibility/exclusion/permission rule, review both the enumeration path and the direct-access path and assert they use ONE shared predicate with identical matching semantics (case, normalization, exact-segment vs substring); add a test that asserts the two agree on a case-variant and on a near-miss name (a file merely *containing* the excluded word must stay reachable). Reachability criteria are not satisfied by testing the listing.

- 2026-07-26 When a new feature roots an EXISTING shared guard at a higher directory than every prior caller, re-audit every rule that guard implements against the widened scope — a latent gap becomes live purely from the change of root, with no edit to the guard itself. And when you fix such a gap by extracting a shared predicate, immediately grep for surviving inline copies and lock the single-sourcing with a test. **Why:** a tree walk skipped dot-entries while the path guard never did; three prior pages rooted at `~/os/knowledge` (0 hidden `.md`) so the hole was unexploitable, but the new page rooted at `~/os` itself exposed 1001 hidden `.md` — `?file=.claude/worktrees/…/README.md` returned 200 with the full body while appearing in no listing. QA had verified the *listing* was correct, which it was; only review caught it. Then the extraction of the shared predicate still left three hand-rolled `name.startsWith('.')` copies in sibling modules. This is the fourth occurrence of lister-vs-guard drift in one repo and the second time the *fix* for it re-created the same class of bug. **How to apply:** treat "same guard, new root" as a scope change requiring a fresh audit, not a no-op reuse; apply the predicate to the path *below* the root (never the root's own absolute path, or every caller whose root legitimately sits under an excluded segment breaks) and on BOTH the pre- and post-realpath paths; then add a cheap grep test asserting the rule appears exactly once in `src/`, and mutation-test it by reintroducing a copy. Pairs with the 2026-07-25 lister/guard bullet — that one says the guard must own the rule; this one says widening the root re-scopes which rules the guard was ever tested against.

- 2026-07-27 Before writing a PLAN.md for a repo, confirm a test runner is actually WIRED — the presence of test files proves nothing, and a `done when:` line reading "existing passing tests remain passing" is vacuous when no command runs them. **Why:** `~/bcns` carried 12 maintained suites in `apps/web/__tests__/*.mjs` (referenced by name in PROGRESS.md as the gate for items B3/B4/W4) while no `package.json` in the repo defined a `test` script and `turbo.json` had no `test` task — the files had only ever been run by hand, one `node --test` at a time. Every regression-hygiene criterion in the new plan would have been unenforceable, and QA would either improvise a command per item or quietly pass the criterion by inspection; both read as green. This is the planning-time twin of the 2026-07-21 "expected pass/skip counts in every spawn prompt" bullet — that one catches a runner that skips silently, this one catches a runner that does not exist. **How to apply:** during plan-md's codebase scan, grep `package.json`/`turbo.json`/CI config for the test entry point and try it once; if nothing runs the suite, make wiring it the FIRST item of the plan (cheap, `light` track) rather than assuming the loop can gate on it. Same probe for `lint`/`typecheck` when criteria lean on them.

- 2026-07-27 A criterion of the form "the script runs EVERY test file" cannot be verified from the runner's own output — `node --test`'s TAP names a file only when the whole file fails or the file is a bare script; files using real `node:test` `test()` calls report subtest NAMES and their filename never appears, so counting filenames in the output undercounts (8 of 12 here) and proves nothing about the other four. The honest gate is **probe injection**: append a deliberately throwing `test()` to each invisible file, run once, and confirm the totals move by exactly the number injected (`tests 52 → 56, fail 4`) with each probe named in the output — this proves execution AND exit-code propagation in one shot. Restore from `cp` backups and verify with `shasum -c`. **Why:** a glob that silently expands to a subset is the exact defect a "wire up the test runner" item exists to prevent, and it is invisible to every casual check — the suite prints a confident green summary either way. **How to apply:** on any item wiring or changing a test entry point, require the probe-injection proof in QA's spawn prompt rather than a filename count, and separately verify non-zero exit from *every* documented entry point (`pnpm --filter x test` AND the root/Turbo one — a task runner can swallow a child's exit code). Corollary from the same run: when an item's premise is "N files are stale for reason R", re-derive it per file before briefing a builder — the handed-down baseline was right for 1 of 5 files (the rest died on a *different* removed key, on placeholder assertions whose slots had since been *filled*, and on a missing build), and building the app first collapsed 10 of 16 failures without touching code.

- 2026-07-26 A library's internal memoization can cache a FAILED parse as a success, so an error path fires only on the FIRST call in a process and silently never again — test every error path TWICE IN ONE PROCESS, not once. **Why:** `gray-matter`'s `matter(raw)` (no options arg) assigns `matter.cache[file.content] = file` at `index.js:47` *before* `parseMatter()` runs at line 50, and gates the whole cache branch on `if (!options)`. When YAML throws, the never-parsed object stays cached: call 1 throws correctly, calls 2+ return `data:{}` with `frontmatterError:null` and `content` still containing the raw `---` fence. A "show a parse-warning row for malformed frontmatter" criterion therefore passed every unit test and every single-request smoke, then displayed a *false* reason from the second HTTP request onward for the life of the server. It shipped one item earlier and was only caught when a later item's acceptance criterion exercised it behaviorally. **How to apply:** pass a truthy options object (`matter(raw, {})`) to opt out — verify it is byte-identical on well-formed input before prescribing it. Generally: when a criterion is "malformed input produces a warning/error", have QA assert it on two consecutive calls in the same process AND on two consecutive requests to the same running server; a fresh-process-per-assertion test suite is structurally blind to this whole class. Suspect it for any parser/loader with a content-keyed cache, and check whether the cache is populated before or after the operation that can throw.

- 2026-07-27 Any test that hand-maintains a mirror of source data — an allowlist of expected tokens, a hardcoded "total fields: N", a duplicated enum — is a sync point that WILL cost a fix cycle, and the correct fix is to derive it from the source at runtime rather than to grow the list. **Why:** a content-registry item added 8 new `[INPUT: ...]` placeholder tokens; the test's hand-listed `APPENDIX_INPUT_TOKENS` false-failed, and the first fixer "solved" it by editing the *copy* (stripping apostrophes so the tokens would match) instead of fixing the test — bending the product around a test's maintenance defect. A neighboring doc had a hand-incremented "Total registry fields: N" that had silently drifted from the real table for two passes (claimed 80, actual 82, then claimed 87 with 88 rows). Both surfaces looked authoritative and neither had ever been re-derived. **How to apply:** in review, flag every literal in a test that restates something the source already knows, and prescribe a runtime walk/count; have QA prove the derivation self-maintains by injecting a brand-new instance and confirming the test still passes with ZERO test edits (and that the derived set is non-empty — a walk that silently collects nothing passes vacuously). When a fix changes a documented count, make QA re-derive it with its own script rather than trusting the fixer's number. Corollary trap: comparing rendered HTML text against source strings requires HTML-entity decoding first (`&#x27;`/`&#39;`/`&quot;`/`&lt;`/`&gt;`/`&amp;`, decoding `&amp;` LAST so double-encoded literals round-trip) — React/JSX escapes rendered text, so any token containing an apostrophe or ampersand can never match its source form raw; and such a scan must be substring-matched, since real registries embed tokens mid-string inside surrounding prose.

- 2026-07-27 An `execFile` arg array stops SHELL injection but not FLAG injection — any user-controlled value appended after a CLI flag whose argument is **optional** (`-r, --resume [value]`, square brackets) is re-parsed as its own flag, so `sessionId:'--dangerously-skip-permissions'` silently disabled an entire agent sandbox. **Why:** the repo had done everything right by convention — arg array, no string interpolation, a UUID regex on the untrusted field — but the regex lived in the HTTP route while argv was built one module away in the lib, so any other caller of the lib escaped it, and nothing in the arg-array rule hints that flag position matters. `claude --help` showing `[value]` rather than `<value>` was the only visible tell. Two sibling lessons from the same item: a permission **deny-rule** block leaves the tool's own denial telemetry (`permission_denials[]`) EMPTY while the model's prose still says "blocked", so telemetry and prose are both worthless — only disk bytes prove it; and `claude --help` piped into a process that exits without draining stdout truncates at 8192 bytes, so a flag-presence drift test reading a pipe passes vacuously (capture through a file fd). **How to apply:** whenever user input becomes a CLI argument, check `--help` for optional-argument (`[...]`) flags and validate **where argv is built**, not at the outer boundary — export one regex and have the boundary import it; then two-side mutation-test the guard (rule on → blocked, rule emptied → the action genuinely succeeds), because a guard that passes for an unrelated reason is indistinguishable from one that works. For any feature granting an agent write access to a config tree, enumerate `ls -la ~/.claude | grep '^l'` (or the equivalent auto-load surfaces) rather than reasoning about which paths matter — a first-pass deny list assembled from memory missed `agents/**` and `CLAUDE.local.md`, both of which persist instructions into every future session machine-wide.

- 2026-07-28 A dt-* agent that returns its findings in its final message but never WRITES its report file leaves the next agent blind, and the failure is silent — the fixer reports "N applied" against only the subset its spawn prompt happened to restate. **Why:** in one run both dt-review and dt-fix skipped the file write; the fixer correctly refused to guess at Important #1–#3 it could not see and flagged the missing work order, but a less careful agent would have reported success with a third of the findings unapplied, and the orchestrator's own summary would have echoed it. The existing "agents don't COMMIT their reports" bullet does not cover this — here the file never existed. Same run, a second failure of the same family: the reviewer's *prescribed constant* was arithmetically wrong (`span >= 100_000` with "all seven ranges satisfy this today"; three actually spanned 47,001/58,001/42,001) and applying it verbatim would have redded the suite. **How to apply:** put `write your report to .claude/dev-team/<name>.md as well as returning it` in every dt-* spawn prompt; after each agent, `ls .claude/dev-team/` and if the file is missing, transcribe it from the agent's returned text into the file yourself before spawning the next one (the orchestrator's context is the only surviving copy). Before dispatching a fixer, check the arithmetic of any numeric constant a reviewer prescribes against the real values in the source — state the corrected value in the fixer's prompt and tell it explicitly not to apply the reviewer's literal number. Corollary that paid off twice: for a "default behaviour must be unchanged" criterion, create the pristine-baseline worktree at the base commit YOURSELF up front and hand its path to QA, so no agent is ever tempted to `git stash` over a tree holding uncommitted teammate work.

- 2026-07-28 On macOS (case-insensitive APFS), a Next.js dynamic route serves **200 for a case-variant slug** and then **corrupts the real prerendered `.html`** — this is a filesystem artifact, not a code defect, and it will read to QA as a genuine security-flavored bug. **Why:** Next's file-system incremental cache `readFile`s `.next/server/app/<route>/<Slug>.html` before the router's `fallback:false` check; on a case-insensitive volume that resolves to the lowercase prerendered file (200, wrong URL live), and the on-demand render's write-back then overwrites it with a 9KB error shell (`fs.existsSync(".../DeLucas.html")` returns `true` for a file never written). On Linux/Vercel the read is ENOENT → cache miss → 404. QA correctly FAILed the item on it; accepting that FAIL would have burned attempts toward a false BLOCKED, and the *tempting* fix — making slug lookup case-insensitive — is actively harmful (it makes `/work/DeLucas` a live duplicate URL, the same lister/guard drift family this repo has shipped four times). **How to apply:** the orchestrator adjudicates this itself in ~3 tool calls before dispatching any fixer — request a mixed-case slug with NO colliding prerendered file (`/work/NoPe`, `/work/DeLucasX`): if those 404 while only the colliding one 200s, it is the filesystem; confirm by moving the colliding `.html` aside and re-requesting the exact failing URL (it will 404), then restore and `shasum -c`. State the adjudication as ground truth in the fixer's AND the re-gating QA's spawn prompt with an explicit "do not re-litigate, case-insensitive matching is forbidden" clause, since QA will otherwise re-FAIL it. `export const dynamicParams = false` is still the correct change on its own merits whenever a route's entire param set is known at compile time. Corollary (sharpens the 2026-07-22 money-path bullet): a reviewer's *prescribed* fix can itself be the defect — here review pass 1 prescribed `<h3>` labels under the new `<h1>` and review pass 2 flagged that exact prescription as a level-skip, costing a round-trip; when a fix is applied verbatim as prescribed, the re-review is checking the reviewer's own judgment, so budget for it rather than treating a prescribed fix as pre-approved.

- 2026-07-28 A "this file can never run against production" criterion is satisfied by CONVENTION until the file guards itself — keeping a fixture out of the auto-applied directory (`supabase/migrations/`, `seed.sql`) only constrains the tools that glob that directory, and says nothing about `psql <any-prod-url> -f fixture.sql`. The honest gate is an in-file opt-in guard verified across **every invocation shape** (`-f`, `< file`, `cat | psql`) with a **row count of zero** asserted on each refusal, not just an absence of output. **Why:** a demo-seed item passed a full containment audit — no `config.toml`, `db push` covers migrations only, `db reset` reads `./seed.sql` only, CI clean — and was still one `psql -f` away from writing 16 invented customers into the client's live DB, next to a tracked `supabase/.temp/pooler-url` that made the connection string a copy-paste away. Worse, the reviewer's own prescribed guard, `\if :{?demo_seed_ok}`, tests whether a psql variable is **defined**, not its value, so `-v demo_seed_ok=0` applied the entire fixture; that hole survived one full review+fix cycle and was caught only by a second fresh review. Related trap from the same item: `psql --set=client_min_messages=warning` is a no-op because `-v/--set` sets a *psql* variable, never a server GUC (use `PGOPTIONS`). **How to apply:** for any item whose criterion is "X can never reach production/real data", require the guard to live in the artifact itself and require the gate to prove BOTH directions per invocation shape — refuses with zero rows written, and applies when opted in. Test guard variables by **value**, and mutation-test the falsy value (`=0`, `=false`, garbage) explicitly, since definedness-vs-value reads identically in review. If the guard's exit code is unreliable (psql's `\quit` exits 0), say so and make every caller assert the rows landed rather than trusting the exit code. Budget a second fresh review on `flag:`-tier containment work — the first fix pass closed 5 findings and introduced this one.

- 2026-07-28 A handed-down environment fact asserting that a local app is SAFELY disconnected from production must be re-verified by the orchestrator from the env file and a reachability probe, never inherited — and the specific trap is an adapter seam that selects production by the mere PRESENCE of a var. **Why:** an item whose whole risk line was "a screenshot capturing a real customer name ships to the public site" arrived with explicit ground truth that the client app's `.env.local` left `NEXT_PUBLIC_SUPABASE_*` blank so there was "no configured path to any production database". The file actually held the live project URL, anon key, **service-role key**, and a production pooler `DATABASE_URL`, and `lib/db/index.ts` checked `NEXT_PUBLIC_SUPABASE_URL` FIRST — so the dev server was serving the client's real customers, and the demo fixture that had just been seeded into the local DB was not being read at all. Two decoys made this look fine: pg-boss logged a connection timeout (the pooler's :6543 is egress-blocked here) and the app printed "email worker disabled: no DB", which reads as "no database configured" — but Supabase REST over :443 was reachable (401 = up), which is the path the adapter actually uses. Cost to catch: one `grep` of the env file plus one `curl`. **How to apply:** whenever an item's risk line is "real data must not reach X", the orchestrator personally reads the actual env/config file and traces the selection function before any agent runs, rather than accepting a prose summary of it — and treats a partial connection failure as evidence about ONE transport, never as proof the app is disconnected. To force a local-only run in Next.js, export the production vars as EMPTY STRINGS (`NEXT_PUBLIC_X=""`), never `unset`/`env -u`: Next repopulates absent keys from `.env.local`, so only present-but-empty wins, and config readers that `.trim()` then treat empty as undefined will fall through to the local adapter. Never edit the user's real `.env.local` to achieve this. Then verify from the OUTPUT (names rendered on the page cross-checked against the local fixture's rows), not from the config you just set.

- 2026-07-28 A criterion of the form "a missing asset must FAIL THE BUILD" is almost never satisfied by the default implementation, because bundlers validate **string** asset paths at no point in the build — only a *static import* creates a checked edge. **Why:** `<Image src="/case-studies/x.png">` in Next.js typechecks, builds, and prerenders happily whether or not the file exists, then 404s into a broken image at runtime — exactly what such a criterion exists to forbid; the same holds for any string path handed to `next/image`, an `<img>`, a CSS `url()`, or a `fetch` of a public asset. Switching to `import img from "@/public/…png"` makes a deleted file an unresolvable module (hard build error) *and* supplies intrinsic width/height so no dimensions get hardcoded — one change buying two criteria. The forcing constraint on where that map can live is worth checking early: here the registry module could not hold the imports because the test suite loads it directly under `node --experimental-strip-types`, which cannot import a `.png`, so a separate module was mandatory rather than stylistic. **How to apply:** when an item says a bad reference must fail the build, the orchestrator should establish *whether the framework validates that reference at all* before any builder runs, and prescribe the static-import (or equivalent compile-time) seam in the spawn prompt; then require QA to mutation-prove **both shapes** — asset deleted (module resolution error) and reference pointed at an unregistered path (prerender throw) — each with a non-zero exit, restored via `cp` + `shasum -c`. Reasoning about this criterion always passes it; only the mutation catches it. Corollary: a build-time throw on a missing lookup is **one-directional** — it catches registry→map drift but an *orphan* map entry with no referent builds perfectly clean, so pair it with a runtime-derived set-equality test (read the map as source TEXT when the test runtime cannot import its binary deps) and mutation-test both directions.
