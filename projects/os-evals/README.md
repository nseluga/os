# os-evals

Real path: `~/os-evals`

**Purpose:** An optimization loop to measure which layers of the ~/os setup earn their keep via ablation ladder: bare Claude → + CLAUDE.md → + memory → + skills.

**How to run:** `cd ~/os-evals && ./run.sh` to execute one full iteration (72 runs across 12 tasks × 4 rungs, plus Opus-tier spot check).

See [os-evals/SPEC.md](../../os-evals/SPEC.md) for complete design.
